# GET LATEST VERSION:
# https://deb.nodesource.com/

# remove old:
sudo apt-get remove -y nodejs libnode-dev

# install new:
cd ~
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs

# check versions:
node -v
npm -v

# global install
sudo npm install -g vsce
sudo npm install node-fetch

# inside extension project folder install nodejs modules
npm install


# with each compile (all of the above is a one time action)
npm run compile
vsce package

# Install the extension and relaod vscode windows
code --install-extension ./aicommit-0.1.2.vsix



# -------------------------------
# old:

sudo apt install npm
sudo npm install
sudo npm install node-fetch@^2
sudo npm install -D @types/node-fetch
sudo npm install -g vsce

npm run compile
vsce package
